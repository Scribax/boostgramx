const express = require('express');
const router = express.Router();

// Placeholder para rutas de pagos
router.get('/', (req, res) => {
  res.json({ message: 'Payments routes - En desarrollo' });
});

module.exports = router;
